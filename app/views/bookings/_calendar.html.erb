<%= simple_form_for [@boat, @booking] do |f| %>
  <%= f.input :date_range, label: 'When would you like to rent?', input_html: { data: { controller: "datepicker" } } %>
  <%= f.button :submit %>
<% end %>
